# Production MySQL (Docker)
# docker-compose will override these with environment variables
spring.datasource.url=jdbc:mysql://${DB_HOST:-localhost}:${DB_PORT:-3306}/${DB_NAME:-lostfound}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME:-root}
spring.datasource.password=${DB_PASSWORD:-rootpassword}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# JWT Configuration
jwt.secret=dGhpcy1pcy1hLXZlcnktc2VjdXJlLTI1Ni1iaXQta2V5LXN0cmluZw==
jwt.expiration=86400000

# Server Configuration - matching frontend .env port
server.port=${APP_PORT:8081}
server.address=0.0.0.0

# CORS - Add your production domains
cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://localhost:3000,http://localhost:8081,https://lostfoundklu1-7.onrender.com

# Google Cloud Vision API
google.cloud.vision.api-key=${GOOGLE_CLOUD_VISION_API_KEY:your_google_cloud_vision_api_key_here}

# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=25MB
spring.servlet.multipart.enabled=true
